[tool.pytest.ini_options]
testpaths = ["tests/backend"]
asyncio_mode = "auto"
python_files = "test_*.py"
python_functions = "test_*"
pythonpath = ["."]
addopts = "--cov=backend --cov-report=term-missing --cov-fail-under=25"
